#include "../../core/basic/adder.h"

#include "gtest/gtest.h"

namespace cop::basic
{
    /* $101$
    TEST(adder, simple)
    {
        const adder instance;

        const auto result = instance.perform_addition(22, 20);

        ASSERT_EQ(42, result) << "wrong answer";
    }
    //*/
}